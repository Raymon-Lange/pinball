# Pinball Project

## Commit messages
Always ask the user to write the commit message. Never write or propose a commit message — stage the files and stop, then let the user run `git commit` themselves.
